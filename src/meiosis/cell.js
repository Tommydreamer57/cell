import React from 'react';
import http from '../http/http';

export default function create(update) {
    // INITIAL DATA
    // CHILDREN
    // METHODS
    // COMPONENT
    return {
        view(model) {
            return (
                <div>

                </div>
            );
        }
    };
}
