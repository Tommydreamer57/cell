import React from 'react';
import http from '../../http';

export default function create(update) {
    // COMPONENT
    return {
        view(model) {
            return (
                <div>

                </div>
            );
        }
    };
}
