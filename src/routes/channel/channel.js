import React from 'react';

export default function create(update) {
    // COMPONENT
    return {
        view(model) {
            return (
                <div id="channel" >
                </div>
            );
        }
    };
}
