import React from 'react';

export default function create(update) {
    return {
        view(model) {
            return (
                <div>
                    
                </div>
            );
        }
    };
}
